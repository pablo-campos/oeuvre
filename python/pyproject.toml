[project]
name = "oeuvre-python"
version = "0.1.0"
description = "Python sandbox for oeuvre playground"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
